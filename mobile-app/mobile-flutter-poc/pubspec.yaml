name: pinpotha
description: PinPotha - Digital පින් පොත iOS App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  firebase_database: ^11.1.3
  firebase_storage: ^12.3.2
  firebase_analytics: ^11.3.3
  
  # Google Sign In
  google_sign_in: ^6.1.6
  
  # State Management
  provider: ^6.1.1
  
  # Image Picker & Camera
  image_picker: ^1.0.7
  camera: ^0.10.5+5
  
  # UI & Navigation
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  pull_to_refresh: ^2.0.0
  
  # Storage
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  
  # Utilities
  intl: ^0.19.0
  share_plus: ^7.2.1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/

